# ---------- Core Framework ----------
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ---------- Data Validation / Settings ----------
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ---------- Database (PostgreSQL / Supabase) ----------
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
supabase==2.8.1

# ---------- Auth / Security ----------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12

# ---------- ML / NLP (Focus-Check Similarity) ----------
sentence-transformers==3.1.1
torch==2.4.1
scikit-learn==1.5.2
numpy==1.26.4

# ---------- LLM Integration (Explainers & Question Generation) ----------
anthropic==0.34.2

# ---------- Real-time (Gap Alerts) ----------
websockets==13.1

# ---------- Push Notifications ----------
firebase-admin==6.5.0

# ---------- HTTP Client (internal service calls) ----------
httpx==0.27.2

# ---------- File / Storage Handling ----------
python-magic==0.4.27

# ---------- CORS / Middleware (built into FastAPI, no extra install needed) ----------

# ---------- Testing ----------
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# ---------- Attendance Utilities ----------
python-dateutil==2.9.0

# ---------- Dev Tools ----------
black==24.8.0
ruff==0.6.9